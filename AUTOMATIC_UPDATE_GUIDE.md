# IRAssistance 자동 업데이트 시스템 가이드

본 문서는 GitHub Releases 및 GitHub Actions를 연동하여 IRAssistance 데스크톱 어플리케이션을 자동으로 빌드·배포하고, 사용자의 프로그램에서 자동으로 감지하여 업데이트를 수행하는 전체 과정에 대해 설명합니다.

---

## 1. 사전 설정 (최초 1회 필수)

GitHub Actions가 빌드를 완료하고 GitHub Releases에 인스톨러 파일을 자동으로 업로드하려면 GitHub 저장소의 권한 설정이 필요합니다.

1. 업데이트가 설정된 GitHub 저장소(`mightyZap25/IRAssistance`)로 이동합니다.
2. 상단 메뉴에서 **Settings** 탭을 클릭합니다.
3. 좌측 사이드바의 **Actions** -> **General**을 클릭합니다.
4. 페이지 하단의 **Workflow permissions** 섹션을 찾습니다.
5. 옵션을 **Read and write permissions**로 변경합니다.
6. **Save** 버튼을 눌러 저장합니다.
   - *이 설정이 되어 있지 않으면 GitHub Actions 빌드는 성공하더라도 배포 파일 업로드 시 권한 에러(HTTP 403)가 발생합니다.*

---

## 2. 신규 버전 배포 프로세스

새로운 기능 개발이나 버그 수정이 완료되어 배포가 필요할 때 다음 단계를 진행합니다.

### Step 1: package.json 버전 수정
`package.json` 파일의 `version` 필드를 새로운 버전으로 변경합니다.
예를 들어 기존 버전이 `0.9.3` 이었다면 `0.9.4` 로 상향합니다.
```json
{
  "name": "ir-assistant",
  "version": "0.9.4",
  ...
}
```

### Step 2: 코드 커밋 및 푸시
수정된 버전과 작업 코드를 메인 브랜치에 커밋하고 푸시합니다.
```bash
git add .
git commit -m "Release v0.9.4: 자동 업데이트 기능 연동 및 고도화"
git push origin main
```

### Step 3: 버전 태그 생성 및 푸시
`package.json`에 입력한 버전명과 동일하게 `v` 접두사를 붙여 git 태그를 생성하고 원격 저장소에 푸시합니다. **이 푸시가 작동하면 GitHub Actions 워크플로우가 자동으로 실행됩니다.**
```bash
git tag v0.9.4
git push origin v0.9.4
```

### Step 4: 빌드 대기 및 Releases 등록
1. 저장소의 **Actions** 탭에서 `Build and Release` 워크플로우가 실행 중인 것을 확인할 수 있습니다. (Windows 가상 환경에서 빌드가 수행되므로 약 3~5분 정도 소요됩니다.)
2. 빌드가 완료되면 저장소 우측의 **Releases** 탭에 `v0.9.4` 버전이 **Draft(임시 저장)** 상태로 자동 생성되어 있으며, 아래에 Windows 설치 파일(`IR Assistant Setup 0.9.4.exe`)과 메타데이터 파일(`latest.yml`)이 첨부되어 있습니다.
3. Draft 릴리즈 페이지에서 수정 사항을 기록한 뒤 **Publish release** 버튼을 클릭하여 공식 배포 상태로 변경합니다. (Publish 상태가 되어야 프로그램이 감지할 수 있습니다.)

---

## 3. 사용자 앱에서의 업데이트 작동 원리

배포가 완료되면 설치되어 동작 중인 구버전 프로그램들에서 다음과 같이 업데이트가 처리됩니다.

### A. 백그라운드 자동 감지
- 프로그램이 켜지고 약 3초 뒤에 백그라운드에서 GitHub Releases의 최신 릴리즈 정보를 조회합니다.
- 로컬 앱 버전보다 릴리즈된 최신 버전이 높다면 화면 중앙에 **"시스템 업데이트 안내" 모달**이 표시됩니다.
- 모달 내에는 신규 버전명, 배포 일자, 릴리즈 노트(변경 사항)가 표시됩니다.

### B. 사용자 선택식 다운로드 및 즉시 설치
1. 사용자가 **"업데이트 다운로드 시작"** 버튼을 누르면 다운로드가 시작되며, 실시간으로 진행률(%)이 표시됩니다.
2. 다운로드가 완료되면 **"지금 설치 및 재시작"** 버튼으로 활성화됩니다.
3. 버튼 클릭 시 앱이 즉시 종료되고 Windows 설치 마법사가 백그라운드에서 실행되어 자동으로 프로그램이 최신 버전으로 덮어씌워진 뒤 다시 실행됩니다.

### C. 수동 업데이트 확인
- 앱의 좌측 메뉴 **설정(Settings)** 페이지 하단에서 현재 프로그램의 버전을 확인할 수 있습니다.
- **"업데이트 확인"** 버튼을 누르면 언제든지 수동으로 새 버전을 체크하여 설치 과정을 밟을 수 있습니다.

---

## 4. 문제 해결 및 팁 (Troubleshooting)

### Q1. 설치 시 "Windows의 PC 보호" (SmartScreen) 경고가 뜹니다.
- **원인**: Microsoft에 등록된 고가의 유료 코드 서명 인증서(Code Signing Certificate)로 설치 파일을 서명하지 않고 빌드했을 때 나타나는 기본 보안 경고입니다.
- **해결책**: 경고 창 내부의 **"추가 정보"** 링크를 누른 다음 우측 하단에 나타나는 **"실행"** 버튼을 누르면 안전하게 설치가 진행됩니다. 사내 내부망 배포나 소규모 배포 시에는 이 방식을 사용하는 것이 일반적입니다.

### Q2. 깃허브 액션 빌드가 실패합니다.
- Actions 로그에서 실패한 단계를 클릭해 상세 에러를 확인합니다.
- 주로 `GITHUB_TOKEN`에 대한 권한 부족(`contents: write`) 또는 `package.json`의 문법 에러 등이 원인이 될 수 있습니다.
